def ajustar_asegurar_con_cf(base_asegurabilidad):

    procesados = 0
    no_procesados = 0

    for fila in base_asegurabilidad:

        ob_mes_vigente = str(
            fila.get("OB MES VIGENTE", "")
        ).strip().lower()

        if ob_mes_vigente == "asegurar con cf":

            fila["OB 3"] = "En renovacion mensual"
            fila["OB MES"] = "Asegurado"
            fila["OBS EDWARD"] = "En renovacion mensual"

            procesados += 1

        else:
            no_procesados += 1

    print("\n=== AJUSTE ASEGURAR CON CF ===")
    print(f"Créditos procesados: {procesados}")
    print(f"Créditos no procesados: {no_procesados}")

    return base_asegurabilidad
